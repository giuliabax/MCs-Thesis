[
  {
    "requirement_id": "PT05",
    "requirement_summary": "Citizen provides details for report classification.",
    "api_endpoint": "/reports",
    "http_method": "POST",
    "prompt": "Create a new report with valid geographic coordinates (lat/lon), a mandatory category from the fixed list, and a detailed description. Ensure the payload includes all required fields for correct classification logic.",
    "test_type": "happy_path",
    "priority": "high",
    "auth_role": "citizen",
    "setup_needed": ["/auth/session (POST) to establish citizen session"],
    "cleanup_strategy": "Verify no resource was created; delete it if unexpectedly present.",
    "expected_status_codes": ["201"],
    "rationale": "Validates the core functionality of report creation and classification based on provided details."
  },
  {
    "requirement_id": "PT05",
    "requirement_summary": "Citizen provides details for report classification.",
    "api_endpoint": "/reports",
    "http_method": "POST",
    "prompt": "Create a new report with valid coordinates and category, but omit the mandatory 'description' field to test classification failure or default handling.",
    "test_type": "edge_case",
    "priority": "medium",
    "auth_role": "citizen",
    "setup_needed": ["/auth/session (POST) to establish citizen session"],
    "cleanup_strategy": "Verify no resource was created; delete it if unexpectedly present.",
    "expected_status_codes": ["400"],
    "rationale": "Tests system behavior when a critical classification detail is missing."
  },
  {
    "requirement_id": "PT05",
    "requirement_summary": "Citizen provides details for report classification.",
    "api_endpoint": "/reports",
    "http_method": "POST",
    "prompt": "Create a new report with invalid geographic coordinates (e.g., lat > 90) and an invalid category string to test input validation.",
    "test_type": "negative",
    "priority": "medium",
    "auth_role": "citizen",
    "setup_needed": ["/auth/session (POST) to establish citizen session"],
    "cleanup_strategy": "Verify no resource was created; delete it if unexpectedly present.",
    "expected_status_codes": ["400"],
    "rationale": "Ensures the API rejects reports with invalid location or category data."
  },
  {
    "requirement_id": "PT08",
    "requirement_summary": "Technical office staff member sees list of assigned reports.",
    "api_endpoint": "/reports/municipality-users/{municipalityUserId}",
    "http_method": "GET",
    "prompt": "Retrieve the list of reports assigned to a specific technical office staff member using their municipality user ID. Ensure the response includes report summaries and current statuses.",
    "test_type": "happy_path",
    "priority": "high",
    "auth_role": "technical office staff member",
    "setup_needed": ["/auth/session (POST) to establish staff session; POST /users/municipality-users to create a test staff user if not present"],
    "cleanup_strategy": "Verify no resource was created; delete it if unexpectedly present.",
    "expected_status_codes": ["200"],
    "rationale": "Validates the overview functionality for technical office staff."
  },
  {
    "requirement_id": "PT08",
    "requirement_summary": "Technical office staff member sees list of assigned reports.",
    "api_endpoint": "/reports/municipality-users/{municipalityUserId}",
    "http_method": "GET",
    "prompt": "Attempt to retrieve the list of reports for a municipality user ID that does not exist or is not associated with any reports.",
    "test_type": "negative",
    "priority": "low",
    "auth_role": "technical office staff member",
    "setup_needed": ["/auth/session (POST) to establish staff session"],
    "cleanup_strategy": "Verify no resource was created; delete it if unexpectedly present.",
    "expected_status_codes": ["404"],
    "rationale": "Tests error handling when querying for a non-existent user or empty assignment list."
  },
  {
    "requirement_id": "PT09",
    "requirement_summary": "Citizen configures account for notifications and virtual presence.",
    "api_endpoint": "/users",
    "http_method": "PATCH",
    "prompt": "Update the user's configuration object to enable email notifications and set a specific virtual presence status (e.g., 'online'). Ensure only allowed fields are modified.",
    "test_type": "happy_path",
    "priority": "high",
    "auth_role": "citizen",
    "setup_needed": ["/auth/session (POST) to establish citizen session"],
    "cleanup_strategy": "Verify no resource was created; delete it if unexpectedly present.",
    "expected_status_codes": ["200"],
    "rationale": "Validates the ability of citizens to manage their notification preferences and presence."
  },
  {
    "requirement_id": "PT09",
    "requirement_summary": "Citizen configures account for notifications and virtual presence.",
    "api_endpoint": "/users",
    "http_method": "PATCH",
    "prompt": "Attempt to update the user's configuration with an invalid field name or a value outside the allowed schema (e.g., setting 'presence' to 'invisible').",
    "test_type": "negative",
    "priority": "medium",
    "auth_role": "citizen",
    "setup_needed": ["/auth/session (POST) to establish citizen session"],
    "cleanup_strategy": "Verify no resource was created; delete it if unexpectedly present.",
    "expected_status_codes": ["400"],
    "rationale": "Ensures the API rejects invalid configuration updates."
  },
  {
    "requirement_id": "PT30",
    "requirement_summary": "Citizen searches for reports in a specific area by typing an address.",
    "api_endpoint": "/reports/search",
    "http_method": "GET",
    "prompt": "Perform a search query using a valid address string (e.g., 'Via Roma, Turin') to retrieve reports located in that specific area. Ensure the response filters correctly.",
    "test_type": "happy_path",
    "priority": "high",
    "auth_role": "citizen/unregistered user",
    "setup_needed": ["/auth/session (POST) to establish session (optional for unregistered users if allowed, otherwise skip auth step)"],
    "cleanup_strategy": "Verify no resource was created; delete it if unexpectedly present.",
    "expected_status_codes": ["200"],
    "rationale": "Validates the search functionality for exploring reports by location."
  },
  {
    "requirement_id": "PT30",
    "requirement_summary": "Citizen searches for reports in a specific area by typing an address.",
    "api_endpoint": "/reports/search",
    "http_method": "GET",
    "prompt": "Perform a search query using an empty string or null address to test behavior when no location is specified.",
    "test_type": "edge_case",
    "priority": "medium",
    "auth_role": "citizen/unregistered user",
    "setup_needed": ["/auth/session (POST) to establish session (optional for unregistered users if allowed, otherwise skip auth step)"],
    "cleanup_strategy": "Verify no resource was created; delete it if unexpectedly present.",
    "expected_status_codes": ["200", "default"],
    "rationale": "Tests how the system handles a search with no specific area filter."
  },
  {
    "requirement_id": "PT18",
    "requirement_summary": "Citizen replies to messages from municipal operators.",
    "api_endpoint": "/reports/{id}/comments/unread",
    "http_method": "GET",
    "prompt": "Retrieve the list of unread comments/messages for a specific report ID. Ensure the response includes message content and sender details.",
    "test_type": "happy_path",
    "priority": "high",
    "auth_role": "citizen",
    "setup_needed": ["/auth/session (POST) to establish citizen session; POST /reports to create a test report with comments if needed"],
    "cleanup_strategy": "Verify no resource was created; delete it if unexpectedly present.",
    "expected_status_codes": ["200"],
    "rationale": "Validates the communication channel between citizens and operators."
  },
  {
    "requirement_id": "PT18",
    "requirement_summary": "Citizen replies to messages from municipal operators.",
    "api_endpoint": "/reports/{id}/comments/unread",
    "http_method": "GET",
    "prompt": "Attempt to retrieve unread comments for a report ID that does not exist or has no comments yet.",
    "test_type": "negative",
    "priority": "low",
    "auth_role": "citizen",
    "setup_needed": ["/auth/session (POST) to establish citizen session"],
    "cleanup_strategy": "Verify no resource was created; delete it if unexpectedly present.",
    "expected_status_codes": ["404"]
  }
]